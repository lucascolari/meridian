# MERIDIAN — Fase 4: Work + transiciones de página — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Construir la sección Work: el listado de trabajos (`/work`), la página de proyecto individual (`/work/[slug]`) como experiencia completa, y las transiciones de página cinematográficas (crossfade + morph card→hero con la View Transitions API), todo alimentado por la capa de contenido y con SEO por proyecto.

**Architecture:** `next-view-transitions` envuelve la app para animar los cambios de ruta; los `Link` internos pasan a su `Link`. El morph card→proyecto se logra con `view-transition-name` compartido entre la portada del listado y el hero del detalle (por slug). El detalle usa `generateStaticParams` + `generateMetadata` + JSON-LD. Se agrega `getAdjacentProject` a la capa de contenido (con test) para "next project".

**Tech Stack:** Next.js 16 App Router, React 19, TS, next-view-transitions, GSAP, next/image, Vitest.

## Global Constraints

- Ubicación `C:\dev\meridian`. NO mezclar con Gular.
- TS estricto, sin `any` sin justificar.
- Todo el motion respeta `prefers-reduced-motion` (las view transitions deben desactivarse/simplificarse con la media query).
- Animar solo transform/opacity/clip-path.
- Contenido desde `@/lib/content` (no duplicar). Copy inglés, voz de estudio, sin rastros de autoría.
- Commits terminan con línea en blanco + exactamente:
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
- `npm run build/typecheck/test/lint` en verde al cerrar cada tarea. `/work` y `/work/[slug]` deben prerenderizarse (SSG).
- YAGNI.

---

### Task 1: `getAdjacentProject` en la capa de contenido (TDD)

**Files:**
- Modify: `src/lib/content/index.ts`
- Modify: `src/lib/content/index.test.ts`

**Interfaces:**
- Produces: `getAdjacentProject(slug: string, dir: "next" | "prev"): Project | undefined` — sobre la lista ordenada por `order` (`getProjects()`), devuelve el proyecto siguiente/anterior con w鴉-around (el siguiente del último es el primero; el anterior del primero es el último). `undefined` si el slug no existe o hay <2 proyectos.

- [ ] **Step 1: Tests (añadir al describe existente)**

```ts
import { getAdjacentProject } from "./index";
// dentro de describe("content layer", ...):
it("getAdjacentProject next envuelve del último al primero", () => {
  const all = getProjects();
  const last = all[all.length - 1];
  expect(getAdjacentProject(last.slug, "next")?.slug).toBe(all[0].slug);
});
it("getAdjacentProject prev envuelve del primero al último", () => {
  const all = getProjects();
  expect(getAdjacentProject(all[0].slug, "prev")?.slug).toBe(all[all.length - 1].slug);
});
it("getAdjacentProject next devuelve el siguiente en orden", () => {
  const all = getProjects();
  expect(getAdjacentProject(all[0].slug, "next")?.slug).toBe(all[1].slug);
});
it("getAdjacentProject con slug inexistente → undefined", () => {
  expect(getAdjacentProject("no-existe", "next")).toBeUndefined();
});
```

- [ ] **Step 2: Correr y verificar que falla**

Run: `npm test -- content`
Expected: FAIL (`getAdjacentProject` no existe).

- [ ] **Step 3: Implementar**

Añadir a `src/lib/content/index.ts`:
```ts
export function getAdjacentProject(
  slug: string,
  dir: "next" | "prev",
): Project | undefined {
  const all = getProjects();
  if (all.length < 2) return undefined;
  const i = all.findIndex((p) => p.slug === slug);
  if (i === -1) return undefined;
  const delta = dir === "next" ? 1 : -1;
  const j = (i + delta + all.length) % all.length;
  return all[j];
}
```

- [ ] **Step 4: Correr y verificar que pasa**

Run: `npm test -- content`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: getAdjacentProject (next/prev con wrap-around) + tests"
```

---

### Task 2: Provider de transiciones de página (next-view-transitions)

**Files:**
- Modify: `package.json` (dep `next-view-transitions`)
- Modify: `src/app/layout.tsx` (envolver con `<ViewTransitions>`)
- Modify: `src/app/globals.css` (CSS de la view transition + guard reduced-motion)
- Create: `src/components/navigation/TransitionLink.tsx` (re-export tipado del `Link` de next-view-transitions)
- Modify: `src/components/navigation/Nav.tsx`, `FullscreenMenu.tsx`, `src/components/sections/FeaturedWork.tsx`, `Welcome.tsx`, `Footer.tsx` (usar `TransitionLink` en vez de `next/link` para navegación interna)

**Interfaces:**
- Produces: navegación interna animada (crossfade suave entre rutas). `TransitionLink` = `export { Link as TransitionLink } from "next-view-transitions";`.

- [ ] **Step 1: Instalar**

Run: `npm install next-view-transitions`
Expected: instala ok.

- [ ] **Step 2: Envolver el layout**

En `src/app/layout.tsx`, importar `{ ViewTransitions }` de `next-view-transitions` y envolver TODO el árbol del `<body>` (por fuera de `<SmoothScroll>`):
```tsx
import { ViewTransitions } from "next-view-transitions";
// ...
<body>
  <ViewTransitions>
    <SmoothScroll>
      <CursorProvider>
        <Cursor />
        <Nav />
        {children}
      </CursorProvider>
    </SmoothScroll>
  </ViewTransitions>
</body>
```

- [ ] **Step 3: `TransitionLink.tsx`**

```tsx
export { Link as TransitionLink } from "next-view-transitions";
```

- [ ] **Step 4: CSS de la transición**

Añadir a `src/app/globals.css`:
```css
@media (prefers-reduced-motion: no-preference) {
  ::view-transition-old(root),
  ::view-transition-new(root) {
    animation-duration: 0.5s;
    animation-timing-function: var(--ease-expo);
  }
}
@media (prefers-reduced-motion: reduce) {
  ::view-transition-old(root),
  ::view-transition-new(root) { animation: none; }
}
```

- [ ] **Step 5: Reemplazar `next/link` por `TransitionLink`** en Nav, FullscreenMenu, FeaturedWork, Welcome, Footer (solo navegación interna a rutas de la app; `mailto:`/externos quedan como `<a>`). Mantener las mismas props.

- [ ] **Step 6: Verificar**

Run: `npm run build && npm run typecheck && npm run lint`
Expected: limpio.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: transiciones de pagina con next-view-transitions"
```

---

### Task 3: Listado `/work`

**Files:**
- Create: `src/app/work/page.tsx`
- Create: `src/components/work/WorkGrid.tsx`
- Create: `src/components/work/WorkCard.tsx`
- Create: `src/components/work/work.module.css`

**Interfaces:**
- Consumes: `getProjects` (`@/lib/content`), `AnimatedImage`, `KineticHeading`, `useCursor`, `TransitionLink`, tipo `Project`.
- Produces: página `/work` (server component con `metadata`), un heading cinético, y `WorkGrid` con un `WorkCard` por proyecto.
  - `WorkCard` (`"use client"` si usa cursor/animación): portada `AnimatedImage` (cover, `cursor="view"`), title, client, category, year; envuelto en `TransitionLink href={/work/${slug}}`. La portada lleva `style={{ viewTransitionName: 'vt-cover-' + slug }}` para el morph al detalle.

- [ ] **Step 1: `page.tsx`** — `export const metadata = buildMetadata({ title: "Work", description: "Selected work by MERIDIAN." })`; layout con heading "Work / Selected projects" + `<WorkGrid projects={getProjects()} />`. Añadir padding superior para no quedar bajo el nav fijo.

- [ ] **Step 2: `WorkCard.tsx` + `WorkGrid.tsx` + `work.module.css`** — grid responsive (1 col mobile, 2 desktop), cards con hover (cursor "view"), meta (client · category · year), `viewTransitionName` en la portada. Reveal on scroll (revealFrom). Accesible (link con texto, alt real).

- [ ] **Step 3: Verificar**

Run: `npm run build && npm run typecheck && npm run lint`
Expected: `/work` prerenderiza; limpio.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: listado /work (grid de proyectos con morph de portada)"
```

---

### Task 4: Detalle `/work/[slug]`

**Files:**
- Create: `src/app/work/[slug]/page.tsx`
- Create: `src/components/work/ProjectHero.tsx`
- Create: `src/components/work/ProjectGallery.tsx`
- Create: `src/components/work/ProjectDetails.tsx`
- Create: `src/components/work/NextProject.tsx`
- Create: `src/components/work/project.module.css`

**Interfaces:**
- Consumes: `getProject`, `getProjects`, `getRelatedProjects`, `getAdjacentProject` (`@/lib/content`), `AnimatedImage`, `KineticHeading`, `RevealText`, `TransitionLink`, `buildMetadata`, `notFound` de `next/navigation`.
- Produces: página de detalle SSG.
  - `generateStaticParams()` → `getProjects().map(p => ({ slug: p.slug }))`.
  - `generateMetadata({ params })` → `buildMetadata` con title = project.title, description = project.excerpt, openGraph.images = [heroMedia.src]; si no existe, metadata base.
  - El componente: si `!getProject(slug)` → `notFound()`. Estructura: `ProjectHero` (heroMedia full-bleed con `viewTransitionName: 'vt-cover-'+slug` para recibir el morph; title, client, year, category), intro (`RevealText` con description), `ProjectGallery` (grid de `gallery` con `AnimatedImage`), `ProjectDetails` (client, year, category, services, credits, awards), related (`getRelatedProjects`) y `NextProject` (`getAdjacentProject(slug,'next')`) como bloque grande al pie que linkea al siguiente.
  - Incluir JSON-LD (`CreativeWork`) via `<script type="application/ld+json">`.

- [ ] **Step 1..N** — implementar la página + los 5 componentes + CSS. `generateStaticParams`/`generateMetadata` correctos. `notFound()` para slug inválido. `viewTransitionName` en el hero coincide con el de la card. JSON-LD válido. Animaciones solo transform/opacity/clip-path, reduced-motion respetado. Verificar `npm run build` (deben prerenderizarse las 6 rutas `/work/<slug>`), `npm run typecheck`, `npm run lint`.

- [ ] **Step final: Commit**

```bash
git add -A
git commit -m "feat: pagina de proyecto /work/[slug] (hero, galeria, detalles, next, JSON-LD, SSG)"
```

---

### Task 5: Verificación de la fase

**Files:** (sin cambios de código salvo fixes que surjan)

- [ ] **Step 1: Suite completa + build**

Run: `npm run build && npm run typecheck && npm test && npm run lint`
Expected: todo verde; en el output de build aparecen `/work` y `● /work/[slug]` (SSG con 6 rutas).

- [ ] **Step 2: Smoke runtime**

`npm run start` en background; `curl` a `/`, `/work`, y `/work/the-arrival` → cada uno 200. Frenar el server.

- [ ] **Step 3 (si hubo fixes): Commit**

```bash
git add -A
git commit -m "test: verificacion Fase 4 (work + transiciones)"
```

---

## Self-Review (writing-plans)

- **Cobertura del spec §15/§16/§24/§25 (work, project detail, SEO por proyecto, page transitions):** Task 1 (next/prev), Task 2 (page transitions + morph infra), Task 3 (listado + morph de portada), Task 4 (detalle completo SSG + generateMetadata + JSON-LD + next project), Task 5 (verificación). Experiencias "immersive/horizontal" adicionales del listado quedan como YAGNI (grid + morph ya cumple).
- **Placeholders:** Task 1 trae código+tests completos; Tasks 3 y 4 van por requisitos (páginas/UI validadas por build+browser). 
- **Consistencia de nombres:** `getAdjacentProject` (Task 1) usado en Task 4; `TransitionLink` (Task 2) usado en Tasks 3/4; `viewTransitionName: 'vt-cover-'+slug` idéntico entre WorkCard (Task 3) y ProjectHero (Task 4); `buildMetadata` (Fase 1) reutilizado en Tasks 3/4.
