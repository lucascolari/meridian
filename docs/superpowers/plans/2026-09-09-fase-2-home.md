# MERIDIAN — Fase 2: Home cinematográfico — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Convertir el home provisional en una experiencia cinematográfica scroll-driven: sistema de animación reutilizable, cursor custom, navegación transformable + fullscreen menu, y las 5 secciones-capítulo (Arrival, Blueprint, Living Spaces, Inner Sanctum, Welcome) con reveals, parallax e imágenes de arquitectura.

**Architecture:** El motion se centraliza en `src/animations/*` (wrappers GSAP configurables que respetan `prefers-reduced-motion`). Los componentes visuales viven en `src/components/{cursor,navigation,typography,media,sections}` y consumen esos wrappers. El home compone las secciones; el layout monta el cursor y la nav. La transición WebGL entre escenas es Fase 3 — acá el efecto cinematográfico se logra con CSS/GSAP (clip-path reveals, scale, parallax).

**Tech Stack:** Next.js 16, React 19, TS estricto, GSAP + ScrollTrigger, Lenis (ya montado), next/image, Vitest.

## Global Constraints

- Ubicación `C:\dev\meridian`. NO mezclar con Gular.
- TS estricto, sin `any` sin justificar.
- **Todo el motion respeta `prefers-reduced-motion`**: con la preferencia activa, los elementos quedan en su estado final visible (nunca ocultos), sin animación ni ScrollTrigger.
- Cursor custom SOLO en desktop/puntero fino; en touch se usa el cursor nativo y no se monta.
- Animar solo `transform`/`opacity`/`clip-path` (GPU-friendly). Nada de `top/left/width/height` animado.
- Marca MERIDIAN, tagline "We build iconic brands.". Copy en inglés, voz de estudio, sin rastros de autoría en código/copy.
- Los mensajes de commit terminan con línea en blanco y luego exactamente:
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
- `npm run build`, `npm run typecheck`, `npm test`, `npm run lint` deben quedar en verde al cerrar cada tarea.
- YAGNI. Componentes chicos y enfocados.

---

### Task 1: Sistema de animación (`src/animations/`)

**Files:**
- Create: `src/animations/config.ts`
- Create: `src/animations/text.ts`
- Create: `src/animations/reveal.ts`
- Create: `src/animations/parallax.ts`
- Create: `src/animations/index.ts`
- Test: `src/animations/text.test.ts`

**Interfaces:**
- Consumes: `registerGsap, gsap, ScrollTrigger` de `@/lib/gsap`.
- Produces:
  - `prefersReducedMotion(): boolean` (SSR-safe: false en server).
  - `MOTION = { duration: { fast: 0.35, medium: 0.7, slow: 1.2 }, ease: { expo: "expo.out", smooth: "power3.out" } }`.
  - `splitIntoWords(text: string): string[]` (pura, testeable).
  - `splitIntoLinesMarkup(el: HTMLElement): void` (envuelve cada palabra en `<span class="word"><span class="word-inner">…</span></span>` para animar).
  - `revealFrom(target, opts?: { y?: number; opacity?: number; duration?: number; ease?: string; delay?: number; stagger?: number; scroll?: boolean | Element; start?: string }): gsap.core.Tween | undefined`.
  - `revealText(target: HTMLElement, opts?: { stagger?: number; duration?: number; ease?: string; scroll?: boolean; start?: string }): void`.
  - `parallax(target, opts?: { amount?: number; start?: string; end?: string }): void`.
  - `index.ts` re-exporta todo.

- [ ] **Step 1: Test de `splitIntoWords`**

`src/animations/text.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { splitIntoWords } from "./text";

describe("splitIntoWords", () => {
  it("divide por espacios y descarta vacíos", () => {
    expect(splitIntoWords("We build iconic")).toEqual(["We", "build", "iconic"]);
  });
  it("colapsa espacios múltiples", () => {
    expect(splitIntoWords("  a   b ")).toEqual(["a", "b"]);
  });
  it("string vacío → array vacío", () => {
    expect(splitIntoWords("   ")).toEqual([]);
  });
});
```

- [ ] **Step 2: Correr y verificar que falla**

Run: `npm test -- text`
Expected: FAIL (módulo no existe).

- [ ] **Step 3: Implementar `config.ts`**

```ts
export const MOTION = {
  duration: { fast: 0.35, medium: 0.7, slow: 1.2 },
  ease: { expo: "expo.out", smooth: "power3.out" },
} as const;

export function prefersReducedMotion(): boolean {
  if (typeof window === "undefined") return false;
  return window.matchMedia("(prefers-reduced-motion: reduce)").matches;
}
```

- [ ] **Step 4: Implementar `text.ts`**

```ts
export function splitIntoWords(text: string): string[] {
  return text.split(/\s+/).filter((w) => w.length > 0);
}

export function splitIntoLinesMarkup(el: HTMLElement): void {
  const words = splitIntoWords(el.textContent ?? "");
  el.textContent = "";
  for (const word of words) {
    const outer = document.createElement("span");
    outer.className = "word";
    outer.style.display = "inline-block";
    outer.style.overflow = "hidden";
    const inner = document.createElement("span");
    inner.className = "word-inner";
    inner.style.display = "inline-block";
    inner.style.willChange = "transform";
    inner.textContent = word;
    outer.appendChild(inner);
    el.appendChild(outer);
    el.appendChild(document.createTextNode(" "));
  }
}
```

- [ ] **Step 5: Implementar `reveal.ts`**

```ts
import { gsap, ScrollTrigger, registerGsap } from "@/lib/gsap";
import { MOTION, prefersReducedMotion } from "./config";

interface RevealOpts {
  y?: number;
  opacity?: number;
  duration?: number;
  ease?: string;
  delay?: number;
  stagger?: number;
  scroll?: boolean | Element;
  start?: string;
}

export function revealFrom(
  target: gsap.TweenTarget,
  opts: RevealOpts = {},
): gsap.core.Tween | undefined {
  if (prefersReducedMotion()) return undefined;
  registerGsap();
  const {
    y = 40,
    opacity = 0,
    duration = MOTION.duration.medium,
    ease = MOTION.ease.expo,
    delay = 0,
    stagger = 0,
    scroll = false,
    start = "top 85%",
  } = opts;
  const scrollTrigger =
    scroll === false
      ? undefined
      : { trigger: scroll === true ? (target as Element) : scroll, start };
  return gsap.from(target, { y, opacity, duration, ease, delay, stagger, scrollTrigger });
}
```

- [ ] **Step 6: Implementar `parallax.ts`**

```ts
import { gsap, registerGsap } from "@/lib/gsap";
import { prefersReducedMotion } from "./config";

interface ParallaxOpts { amount?: number; start?: string; end?: string }

export function parallax(target: gsap.TweenTarget, opts: ParallaxOpts = {}): void {
  if (prefersReducedMotion()) return;
  registerGsap();
  const { amount = 100, start = "top bottom", end = "bottom top" } = opts;
  gsap.to(target, {
    yPercent: amount / 10,
    ease: "none",
    scrollTrigger: { trigger: target as Element, start, end, scrub: true },
  });
}
```

- [ ] **Step 7: Implementar `text.ts` reveal + `index.ts`**

Añadir a `text.ts`:
```ts
import { gsap, ScrollTrigger, registerGsap } from "@/lib/gsap";
import { MOTION, prefersReducedMotion } from "./config";

interface RevealTextOpts { stagger?: number; duration?: number; ease?: string; scroll?: boolean; start?: string }

export function revealText(el: HTMLElement, opts: RevealTextOpts = {}): void {
  splitIntoLinesMarkup(el);
  if (prefersReducedMotion()) return;
  registerGsap();
  const { stagger = 0.06, duration = MOTION.duration.medium, ease = MOTION.ease.expo, scroll = true, start = "top 85%" } = opts;
  gsap.from(el.querySelectorAll(".word-inner"), {
    yPercent: 120,
    duration,
    ease,
    stagger,
    scrollTrigger: scroll ? { trigger: el, start } : undefined,
  });
}
```

`src/animations/index.ts`:
```ts
export * from "./config";
export * from "./text";
export * from "./reveal";
export * from "./parallax";
```

- [ ] **Step 8: Correr test + verificaciones**

Run:
```bash
npm test -- text
npm run typecheck
npm run build
npm run lint
```
Expected: test PASS; typecheck/build/lint limpios.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "feat: sistema de animacion (reveal/text/parallax) con reduced-motion"
```

---

### Task 2: Cursor custom (`src/components/cursor/`)

**Files:**
- Create: `src/components/cursor/CursorProvider.tsx`
- Create: `src/components/cursor/Cursor.tsx`
- Create: `src/hooks/useCursor.ts`
- Create: `src/styles/cursor.css`
- Modify: `src/app/globals.css` (importar cursor.css)

**Interfaces:**
- Consumes: `useDeviceCapabilities` (`@/hooks/useDeviceCapabilities`), `gsap` (`@/lib/gsap`).
- Produces:
  - `CursorVariant = "default" | "view" | "play" | "explore" | "drag"`.
  - `<CursorProvider>{children}</CursorProvider>` (contexto con `{ variant, label, setCursor(variant, label?) , reset() }`).
  - `useCursor(): { setCursor: (v: CursorVariant, label?: string) => void; reset: () => void }`.
  - `<Cursor />` (renderiza el cursor; no monta nada si `isTouch`).

- [ ] **Step 1: `useCursor` + contexto en `CursorProvider.tsx`**

```tsx
"use client";
import { createContext, useContext, useMemo, useState } from "react";
import type { ReactNode } from "react";

export type CursorVariant = "default" | "view" | "play" | "explore" | "drag";

interface CursorState {
  variant: CursorVariant;
  label: string;
  setCursor: (variant: CursorVariant, label?: string) => void;
  reset: () => void;
}

const CursorContext = createContext<CursorState | null>(null);

export function CursorProvider({ children }: { children: ReactNode }) {
  const [variant, setVariant] = useState<CursorVariant>("default");
  const [label, setLabel] = useState("");
  const value = useMemo<CursorState>(
    () => ({
      variant,
      label,
      setCursor: (v, l = "") => { setVariant(v); setLabel(l); },
      reset: () => { setVariant("default"); setLabel(""); },
    }),
    [variant, label],
  );
  return <CursorContext.Provider value={value}>{children}</CursorContext.Provider>;
}

export function useCursorContext(): CursorState {
  const ctx = useContext(CursorContext);
  if (!ctx) throw new Error("useCursor must be used within CursorProvider");
  return ctx;
}
```

`src/hooks/useCursor.ts`:
```ts
"use client";
import { useCursorContext } from "@/components/cursor/CursorProvider";

export function useCursor() {
  const { setCursor, reset } = useCursorContext();
  return { setCursor, reset };
}
```

- [ ] **Step 2: `Cursor.tsx`**

```tsx
"use client";
import { useEffect, useRef } from "react";
import { gsap } from "@/lib/gsap";
import { useDeviceCapabilities } from "@/hooks/useDeviceCapabilities";
import { useCursorContext } from "./CursorProvider";

export function Cursor() {
  const { isTouch } = useDeviceCapabilities();
  const { variant, label } = useCursorContext();
  const dotRef = useRef<HTMLDivElement>(null);
  const ringRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (isTouch) return;
    const dot = dotRef.current;
    const ring = ringRef.current;
    if (!dot || !ring) return;
    const xDot = gsap.quickTo(dot, "x", { duration: 0.15, ease: "power3" });
    const yDot = gsap.quickTo(dot, "y", { duration: 0.15, ease: "power3" });
    const xRing = gsap.quickTo(ring, "x", { duration: 0.4, ease: "power3" });
    const yRing = gsap.quickTo(ring, "y", { duration: 0.4, ease: "power3" });
    const move = (e: PointerEvent) => {
      xDot(e.clientX); yDot(e.clientY); xRing(e.clientX); yRing(e.clientY);
    };
    window.addEventListener("pointermove", move);
    return () => window.removeEventListener("pointermove", move);
  }, [isTouch]);

  if (isTouch) return null;

  return (
    <div className="cursor" data-variant={variant} aria-hidden="true">
      <div className="cursor-dot" ref={dotRef} />
      <div className="cursor-ring" ref={ringRef}>
        {label ? <span className="cursor-label">{label}</span> : null}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: `src/styles/cursor.css`**

```css
@media (hover: hover) and (pointer: fine) {
  * { cursor: none; }
}
.cursor { position: fixed; inset: 0; pointer-events: none; z-index: var(--z-cursor); }
.cursor-dot, .cursor-ring {
  position: fixed; top: 0; left: 0; transform: translate(-50%, -50%);
  border-radius: 999px; will-change: transform;
}
.cursor-dot { width: 6px; height: 6px; background: var(--bone); }
.cursor-ring {
  width: 40px; height: 40px; border: 1px solid color-mix(in srgb, var(--bone) 50%, transparent);
  display: grid; place-items: center; transition: width .3s var(--ease-expo), height .3s var(--ease-expo), background .3s;
}
.cursor[data-variant="view"] .cursor-ring,
.cursor[data-variant="explore"] .cursor-ring,
.cursor[data-variant="play"] .cursor-ring { width: 80px; height: 80px; background: color-mix(in srgb, var(--gold) 20%, transparent); border-color: var(--gold); }
.cursor-label { font-size: 10px; letter-spacing: .15em; text-transform: uppercase; color: var(--bone); }
@media (prefers-reduced-motion: reduce) {
  .cursor-dot, .cursor-ring { transition: none; }
}
```

Añadir a `src/app/globals.css` tras los imports existentes: `@import "../styles/cursor.css";`

- [ ] **Step 4: Verificar**

Run: `npm run build && npm run typecheck && npm run lint`
Expected: limpio.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: cursor custom con estados (desktop only)"
```

---

### Task 3: Navegación (`src/components/navigation/`)

**Files:**
- Create: `src/components/navigation/Nav.tsx`
- Create: `src/components/navigation/FullscreenMenu.tsx`
- Create: `src/components/navigation/nav.module.css`

**Interfaces:**
- Consumes: `getSiteSettings` (`@/lib/content`), `useCursor`, `useLenis` (`@/hooks/useLenis`), `next/link`.
- Produces: `<Nav />` (server-friendly wrapper que renderiza el client nav). Header fijo con logo MERIDIAN + links Work/About/Contact + botón Menu. Al hacer scroll >80px agrega estado compacto (clase). `<FullscreenMenu open onClose>` overlay animado con links, selected work (featured) y social.

Requisitos:
- Nav es `"use client"`; usa `window scroll` o ScrollTrigger para el estado compacto.
- El menú fullscreen: al abrir bloquea el scroll de Lenis (`useLenis()?.stop()`), al cerrar lo reanuda (`start()`); animación de entrada rápida (<0.6s) con stagger de links; respeta reduced-motion (aparece sin animación).
- Accesible: botón con `aria-expanded`, cerrar con `Escape`, focus visible, links reales.
- Estilos en CSS module; usa tokens.

- [ ] **Step 1: `nav.module.css`** — header fijo (`position: fixed; top:0; width:100%; z-index: var(--z-nav)`), transición de padding/backdrop en `.compact`, layout flfor logo/links/menu, y overlay fullscreen (`position: fixed; inset:0; background: var(--ink); z-index: var(--z-overlay)`). Usar solo tokens; animar opacity/transform.

- [ ] **Step 2: `FullscreenMenu.tsx`** — `"use client"`. Props `{ open: boolean; onClose: () => void }`. Renderiza overlay con nav links (Work/About/Contact), lista de `getFeaturedProjects()` como "Selected work", y social de `getSiteSettings()`. Maneja `Escape`, focus, y anima entrada/salida con gsap (guardado por reduced-motion). Bloquea/reanuda Lenis.

- [ ] **Step 3: `Nav.tsx`** — `"use client"`. Logo `MERIDIAN` (Link a `/`), links, botón Menu que abre `FullscreenMenu`. Estado compacto por scroll. Integra `useCursor` en el logo/links (`setCursor("explore")` en hover del botón menu, opcional). 

- [ ] **Step 4: Verificar**

Run: `npm run build && npm run typecheck && npm run lint`
Expected: limpio (los componentes aún no se montan en el layout; se hará en Task 6).

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: navegacion transformable + fullscreen menu"
```

---

### Task 4: Componentes de tipografía y media

**Files:**
- Create: `src/components/typography/KineticHeading.tsx`
- Create: `src/components/typography/RevealText.tsx`
- Create: `src/components/media/AnimatedImage.tsx`
- Create: `src/components/typography/typography.module.css`
- Create: `src/components/media/media.module.css`

**Interfaces:**
- Consumes: `revealText, revealFrom, parallax` (`@/animations`), `useCursor`, `next/image`, tipo `Media` (`@/types`).
- Produces:
  - `<KineticHeading as?="h1"|"h2"|"h3" className? >{text}</KineticHeading>` — cliente; en mount aplica `revealText` al elemento; texto en display serif.
  - `<RevealText as?="p" className?>{children}</RevealText>` — cliente; aplica `revealFrom` on scroll.
  - `<AnimatedImage media parallax?=boolean priority?=boolean sizes? className? cursor?="view"|"explore" >` — `next/image` con reveal por clip-path (animar `clip-path`/scale con gsap on scroll), parallax opcional, y set de cursor en hover si `cursor` está definido.

Requisitos: todos `"use client"`, usan `useEffect` + `useRef`, respetan reduced-motion (heredado de los wrappers). `AnimatedImage` usa `fill` o width/height del `Media`; contenedor con `overflow:hidden` para el clip-path reveal.

- [ ] **Step 1..N** — implementar los 3 componentes + sus módulos CSS, con reveal por clip-path en `AnimatedImage` (ej: gsap.from con `clipPath: "inset(100% 0 0 0)"` → `inset(0% 0 0 0)`, guardado por reduced-motion). Verificar con `npm run build && npm run typecheck && npm run lint`.

- [ ] **Step final: Commit**

```bash
git add -A
git commit -m "feat: componentes de tipografia cinetica + imagen animada"
```

---

### Task 5: Secciones-capítulo del home (`src/components/sections/`)

**Files:**
- Create: `src/components/sections/Hero.tsx`            (THE ARRIVAL)
- Create: `src/components/sections/Blueprint.tsx`       (servicios)
- Create: `src/components/sections/FeaturedWork.tsx`    (LIVING SPACES)
- Create: `src/components/sections/Statement.tsx`       (INNER SANCTUM)
- Create: `src/components/sections/Welcome.tsx`         (contacto/CTA)
- Create: `src/components/sections/Footer.tsx`
- Create: `src/components/sections/sections.module.css`

**Interfaces:**
- Consumes: `getSiteSettings, getServices, getFeaturedProjects` (`@/lib/content`), `KineticHeading, RevealText, AnimatedImage`, `revealFrom, parallax`, `useCursor`, `next/link`.
- Produces: seis componentes de sección (cada uno `<section>` semántico con `id` del capítulo: `arrival, blueprint, living, sanctum, welcome`). Cada sección se auto-contiene (data + layout + motion).

Contenido/diseño (voz de estudio, inglés):
- **Hero (arrival):** viewport completo; imagen de fondo del primer featured (`AnimatedImage` con leve scale on scroll/parallax), overlay oscuro, `MERIDIAN` gigante (KineticHeading), tagline, y una línea "Scroll" abajo. Etiqueta pequeña "THE ARRIVAL — Creative Studio".
- **Blueprint:** título "The Blueprint", intro breve, y `getServices()` como lista numerada (01–04) con título + summary + capabilities; reveal escalonado.
- **FeaturedWork (living):** título "Living Spaces / Selected Work"; `getFeaturedProjects()` como cards grandes alternadas (imagen `AnimatedImage` con parallax + cursor "view"), con client/category/year; cada card linkea a `/work/<slug>` (la página llega en Fase 4; el link puede existir).
- **Statement (sanctum):** un statement grande tipo manifiesto con `KineticHeading` (ej: "We build brands that feel inevitable."), fondo casi negro.
- **Welcome:** CTA de contacto grande ("Let's build something iconic."), email de `getSiteSettings()` como link `mailto:`, y link a `/contact`.
- **Footer:** logo, nav mínima, social, año dinámico, tagline.

- [ ] **Step 1..N** — implementar las 6 secciones + CSS module. Usar solo `transform/opacity/clip-path`. Verificar `npm run build && npm run typecheck && npm run lint`.

- [ ] **Step final: Commit**

```bash
git add -A
git commit -m "feat: secciones-capitulo del home (arrival/blueprint/living/sanctum/welcome + footer)"
```

---

### Task 6: Composición del home + montaje en el layout

**Files:**
- Modify: `src/app/page.tsx` (home real)
- Modify: `src/app/layout.tsx` (montar CursorProvider + Cursor + Nav dentro de SmoothScroll)

**Interfaces:**
- Consumes: todas las secciones (Task 5), `Nav` (Task 3), `CursorProvider` + `Cursor` (Task 2).
- Produces: home cinematográfico completo; layout con cursor y nav globales.

- [ ] **Step 1: `page.tsx`** — reemplazar el home provisional por la composición de secciones:
```tsx
import { Hero } from "@/components/sections/Hero";
import { Blueprint } from "@/components/sections/Blueprint";
import { FeaturedWork } from "@/components/sections/FeaturedWork";
import { Statement } from "@/components/sections/Statement";
import { Welcome } from "@/components/sections/Welcome";
import { Footer } from "@/components/sections/Footer";

export default function HomePage() {
  return (
    <>
      <Hero />
      <Blueprint />
      <FeaturedWork />
      <Statement />
      <Welcome />
      <Footer />
    </>
  );
}
```

- [ ] **Step 2: `layout.tsx`** — envolver el árbol en `CursorProvider`, montar `<Cursor />` y `<Nav />` dentro de `<SmoothScroll>`:
```tsx
import { CursorProvider } from "@/components/cursor/CursorProvider";
import { Cursor } from "@/components/cursor/Cursor";
import { Nav } from "@/components/navigation/Nav";
// ...
<body>
  <SmoothScroll>
    <CursorProvider>
      <Cursor />
      <Nav />
      {children}
    </CursorProvider>
  </SmoothScroll>
</body>
```

- [ ] **Step 3: Verificar build + suite + lint**

Run: `npm run build && npm run typecheck && npm test && npm run lint`
Expected: todo verde; home compila.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: composicion del home cinematografico + cursor/nav globales"
```

---

## Self-Review (writing-plans)

- **Cobertura del spec §2/§6/§10/§11/§12 (home, animación, tipografía, cursor, nav, secciones):** Task 1 (sistema de animación), Task 2 (cursor + estados + touch off), Task 3 (nav transformable + fullscreen menu + a11y), Task 4 (tipografía cinética + imagen animada), Task 5 (5 capítulos + footer), Task 6 (composición + montaje). WebGL scene transition = Fase 3 (fuera de alcance acá, declarado).
- **Placeholders:** Tasks 3/4/5 describen componentes visuales con interfaces y requisitos concretos en vez de código íntegro (son piezas de UI/DOM que se validan por build + browser, no por lógica pura). Task 1 sí trae el código completo de los wrappers y su único test de lógica pura (`splitIntoWords`).
- **Consistencia de tipos/nombres:** `useCursor().setCursor(variant,label)` definido en Task 2 y consumido en Tasks 3/4/5. Wrappers `revealFrom/revealText/parallax` definidos en Task 1 y consumidos en Tasks 4/5. `CursorProvider/Cursor/Nav` definidos en Tasks 2/3 y montados en Task 6. IDs de capítulo (`arrival/blueprint/living/sanctum/welcome`) coinciden con `siteSettings.chapters`.
