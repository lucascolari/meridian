# MERIDIAN — Fase 3: Efecto estrella WebGL — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Construir la transición cinematográfica entre escenas con WebGL: un `<SceneTransition>` reutilizable (shader de displacement + RGB shift) montado en una sección "Showcase" pineada y scroll-driven que morphea entre imágenes de arquitectura (reproduciendo el morph del reel), con degradación a crossfade CSS en mobile / low-end / reduced-motion.

**Architecture:** Un único `<Canvas>` (R3F) montado por dynamic import (`ssr:false`), lazy. Un plano cubre el viewport con un `ShaderMaterial`; el scroll (GSAP ScrollTrigger pin+scrub) escribe un `progressRef` que `useFrame` lee para actualizar los uniforms (sin re-render de React por frame). Capability gating decide WebGL vs fallback CSS. WebGL se usa acá porque el morph líquido entre imágenes no se logra con CSS/GSAP — exactamente donde Three.js aporta.

**Tech Stack:** Next.js 16, React 19, TS estricto, three, @react-three/fiber, @react-three/drei, GSAP ScrollTrigger, Vitest.

## Global Constraints

- Ubicación `C:\dev\meridian`. NO mezclar con Gular.
- TS estricto, sin `any` sin justificar.
- **WebGL solo cuando aporta.** Degradar a crossfade CSS (mismo resultado narrativo) en `isTouch`, `isLowEnd` o `prefersReducedMotion`.
- Un solo `<Canvas>`, dynamic import `ssr:false`, dpr limitado por `useDeviceCapabilities().dpr`.
- Animar por uniforms/scrub; nada de layout thrashing. Objetivo 60fps.
- Sin rastros de autoría en código/copy. Commits terminan con línea en blanco + exactamente:
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
- `npm run build/typecheck/test/lint` en verde al cerrar cada tarea.
- YAGNI.

---

### Task 1: Dependencias 3D + shaders + util de progreso

**Files:**
- Modify: `package.json` (deps 3D)
- Create: `src/shaders/vertex/plane.vert.ts`
- Create: `src/shaders/fragment/displacement.frag.ts`
- Create: `src/components/webgl/progress.ts`
- Test: `src/components/webgl/progress.test.ts`

**Interfaces:**
- Produces:
  - Deps instaladas: `three`, `@react-three/fiber`, `@react-three/drei`, y `@types/three` (dev).
  - `planeVertexShader: string`, `displacementFragmentShader: string`.
  - `interface ProgressPair { from: number; to: number; t: number }`.
  - `mapProgress(progress: number, count: number): ProgressPair` — dado un progreso 0..1 y N imágenes, devuelve el par de índices a mezclar y el `t` local 0..1.

- [ ] **Step 1: Instalar deps 3D**

Run (en `C:\dev\meridian`):
```bash
npm install three @react-three/fiber @react-three/drei
npm install -D @types/three
```
Expected: instala sin errores.

- [ ] **Step 2: Test de `mapProgress`**

`src/components/webgl/progress.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { mapProgress } from "./progress";

describe("mapProgress", () => {
  it("progreso 0 → par (0,1) t=0", () => {
    expect(mapProgress(0, 5)).toEqual({ from: 0, to: 1, t: 0 });
  });
  it("progreso 1 → último par con t=1", () => {
    expect(mapProgress(1, 5)).toEqual({ from: 3, to: 4, t: 1 });
  });
  it("progreso medio en 3 imágenes → (1,2) t=0", () => {
    expect(mapProgress(0.5, 3)).toEqual({ from: 1, to: 2, t: 0 });
  });
  it("clampa fuera de rango", () => {
    expect(mapProgress(-1, 4)).toEqual({ from: 0, to: 1, t: 0 });
    expect(mapProgress(2, 4)).toEqual({ from: 2, to: 3, t: 1 });
  });
  it("count<=1 → par (0,0) t=0", () => {
    expect(mapProgress(0.7, 1)).toEqual({ from: 0, to: 0, t: 0 });
  });
});
```

- [ ] **Step 3: Correr y verificar que falla**

Run: `npm test -- progress`
Expected: FAIL (módulo no existe).

- [ ] **Step 4: Implementar `progress.ts`**

```ts
export interface ProgressPair {
  from: number;
  to: number;
  t: number;
}

export function mapProgress(progress: number, count: number): ProgressPair {
  if (count <= 1) return { from: 0, to: 0, t: 0 };
  const clamped = Math.min(Math.max(progress, 0), 1);
  const scaled = clamped * (count - 1);
  const from = Math.min(Math.floor(scaled), count - 2);
  const to = from + 1;
  const t = scaled - from;
  return { from, to, t };
}
```

- [ ] **Step 5: Implementar shaders**

`src/shaders/vertex/plane.vert.ts`:
```ts
export const planeVertexShader = /* glsl */ `
  varying vec2 vUv;
  void main() {
    vUv = uv;
    gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
  }
`;
```

`src/shaders/fragment/displacement.frag.ts`:
```ts
export const displacementFragmentShader = /* glsl */ `
  precision highp float;
  uniform sampler2D uTexture0;
  uniform sampler2D uTexture1;
  uniform float uProgress;
  uniform float uDisplacement;
  uniform float uRgbShift;
  uniform vec2 uResolution;
  uniform vec2 uTex0Size;
  uniform vec2 uTex1Size;
  varying vec2 vUv;

  // "cover" fit: llena el plano sin deformar la imagen
  vec2 coverUv(vec2 uv, vec2 res, vec2 img) {
    vec2 s = res / img;
    float scale = max(s.x, s.y);
    vec2 size = img * scale;
    vec2 offset = (res - size) * 0.5 / res;
    vec2 ratio = size / res;
    return (uv - offset) / ratio;
  }

  void main() {
    vec2 uv0 = coverUv(vUv, uResolution, uTex0Size);
    vec2 uv1 = coverUv(vUv, uResolution, uTex1Size);
    float p = smoothstep(0.0, 1.0, uProgress);
    float wave = sin(p * 3.14159265);
    vec2 disp = vec2(0.0, wave * uDisplacement);
    float shift = uRgbShift * wave;

    vec4 c0 = texture2D(uTexture0, uv0 + disp);
    vec4 c1 = texture2D(uTexture1, uv1 - disp);
    c0.r = texture2D(uTexture0, uv0 + disp + vec2(shift, 0.0)).r;
    c1.r = texture2D(uTexture1, uv1 - disp + vec2(shift, 0.0)).r;

    gl_FragColor = mix(c0, c1, p);
  }
`;
```

- [ ] **Step 6: Correr test + verificaciones**

Run:
```bash
npm test -- progress
npm run typecheck
npm run build
npm run lint
```
Expected: test PASS; typecheck/build/lint limpios.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: deps 3D + shaders de displacement + util de progreso (test)"
```

---

### Task 2: SceneTransition + SceneCanvas (WebGL)

**Files:**
- Create: `src/components/webgl/SceneTransition.tsx`
- Create: `src/components/webgl/SceneCanvas.tsx`

**Interfaces:**
- Consumes: `three`, `@react-three/fiber` (`Canvas`, `useFrame`, `useThree`), `@react-three/drei` (`useTexture`), shaders (Task 1), `mapProgress` (Task 1), `useDeviceCapabilities`.
- Produces:
  - `<SceneTransition images={string[]} progressRef={{ current: number }} />` — plano que cubre el viewport con el ShaderMaterial; en `useFrame` lee `progressRef.current`, calcula el par con `mapProgress`, y actualiza uniforms (`uTexture0/1`, `uTex0Size/1Size`, `uProgress`, `uResolution`).
  - `<SceneCanvas images={string[]} progressRef={{ current: number }} />` — `<Canvas>` (dpr de capabilities, `gl={{ antialias:false }}`), monta `<SceneTransition>` en `<Suspense>`.

- [ ] **Step 1: `SceneTransition.tsx`**

```tsx
"use client";
import { useMemo, useRef } from "react";
import * as THREE from "three";
import { useFrame, useThree } from "@react-three/fiber";
import { useTexture } from "@react-three/drei";
import { planeVertexShader } from "@/shaders/vertex/plane.vert";
import { displacementFragmentShader } from "@/shaders/fragment/displacement.frag";
import { mapProgress } from "./progress";

interface Props {
  images: string[];
  progressRef: React.RefObject<number>;
}

export function SceneTransition({ images, progressRef }: Props) {
  const textures = useTexture(images) as THREE.Texture[];
  const { viewport, size } = useThree();
  const materialRef = useRef<THREE.ShaderMaterial>(null);

  const uniforms = useMemo(
    () => ({
      uTexture0: { value: textures[0] ?? null },
      uTexture1: { value: textures[1] ?? textures[0] ?? null },
      uProgress: { value: 0 },
      uDisplacement: { value: 0.12 },
      uRgbShift: { value: 0.01 },
      uResolution: { value: new THREE.Vector2(size.width, size.height) },
      uTex0Size: { value: new THREE.Vector2(1, 1) },
      uTex1Size: { value: new THREE.Vector2(1, 1) },
    }),
    // solo al montar; las texturas se asignan en useFrame
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [],
  );

  const sizeOf = (t: THREE.Texture | undefined): [number, number] => {
    const img = t?.image as { width?: number; height?: number } | undefined;
    return [img?.width ?? 1, img?.height ?? 1];
  };

  useFrame(() => {
    const mat = materialRef.current;
    if (!mat) return;
    const { from, to, t } = mapProgress(progressRef.current ?? 0, textures.length);
    const tex0 = textures[from];
    const tex1 = textures[to];
    mat.uniforms.uTexture0.value = tex0 ?? null;
    mat.uniforms.uTexture1.value = tex1 ?? tex0 ?? null;
    const [w0, h0] = sizeOf(tex0);
    const [w1, h1] = sizeOf(tex1);
    mat.uniforms.uTex0Size.value.set(w0, h0);
    mat.uniforms.uTex1Size.value.set(w1, h1);
    mat.uniforms.uResolution.value.set(size.width, size.height);
    mat.uniforms.uProgress.value = t;
  });

  return (
    <mesh scale={[viewport.width, viewport.height, 1]}>
      <planeGeometry args={[1, 1]} />
      <shaderMaterial
        ref={materialRef}
        vertexShader={planeVertexShader}
        fragmentShader={displacementFragmentShader}
        uniforms={uniforms}
      />
    </mesh>
  );
}
```

- [ ] **Step 2: `SceneCanvas.tsx`**

```tsx
"use client";
import { Suspense } from "react";
import { Canvas } from "@react-three/fiber";
import { useDeviceCapabilities } from "@/hooks/useDeviceCapabilities";
import { SceneTransition } from "./SceneTransition";

interface Props {
  images: string[];
  progressRef: React.RefObject<number>;
}

export function SceneCanvas({ images, progressRef }: Props) {
  const { dpr } = useDeviceCapabilities();
  return (
    <Canvas
      dpr={dpr}
      gl={{ antialias: false, powerPreference: "high-performance" }}
      camera={{ position: [0, 0, 1], fov: 50 }}
      style={{ position: "absolute", inset: 0 }}
    >
      <Suspense fallback={null}>
        <SceneTransition images={images} progressRef={progressRef} />
      </Suspense>
    </Canvas>
  );
}
```

- [ ] **Step 3: Verificar**

Run: `npm run typecheck && npm run build && npm run lint`
Expected: limpio (aún no montado en el home).

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: SceneTransition (shader displacement) + SceneCanvas (R3F lazy)"
```

---

### Task 3: Sección Showcase (pin + scroll) + fallback CSS

**Files:**
- Create: `src/components/sections/Showcase.tsx`
- Create: `src/components/sections/ShowcaseFallback.tsx`
- Create: `src/components/sections/showcase.module.css`

**Interfaces:**
- Consumes: `getProjects`, `getSiteSettings` (`@/lib/content`), `useDeviceCapabilities`, `gsap`+`ScrollTrigger`+`registerGsap` (`@/lib/gsap`), `SceneCanvas` (vía dynamic import `ssr:false`), `prefersReducedMotion`.
- Produces:
  - `<Showcase />` — `<section id="showcase">` pineada; un wrapper de altura `N*100vh` que al hacer scroll actualiza `progressRef.current` (ScrollTrigger `pin` + `scrub`) y muestra las etiquetas de capítulo animadas encima del canvas. Elige `images` = heroMedia.src de los primeros 5 proyectos; `labels` = `getSiteSettings().chapters` (5).
  - Si `isTouch || isLowEnd || prefersReducedMotion()` → renderiza `<ShowcaseFallback images labels />` (crossfade CSS scroll-driven, sin WebGL).

Requisitos:
- El canvas WebGL se carga con `const SceneCanvas = dynamic(() => import("@/components/webgl/SceneCanvas").then(m => m.SceneCanvas), { ssr: false })`.
- `progressRef = useRef(0)`. En `useEffect` (client), registrar un ScrollTrigger sobre el contenedor: `{ trigger, start: "top top", end: () => "+=" + (images.length * 100) + "%", pin: pinTarget, scrub: true, onUpdate: (self) => { progressRef.current = self.progress; setActive(...) } }`. Limpiar el ScrollTrigger en el cleanup. Guardar por reduced-motion (no se llega acá si se usó fallback).
- Etiqueta activa: derivar el índice de capítulo del progreso (con `mapProgress` o Math.round) y resaltarla; usar solo opacity/transform.
- El `<section>` ocupa `height: (images.length * 100)vh`; el contenido pineado ocupa `100vh` con el canvas en `position:absolute; inset:0` y las labels encima.
- Overlay sutil (gradiente) para legibilidad de las labels.
- **ShowcaseFallback**: misma estructura pineada conceptual pero sin WebGL: una pila de imágenes `next/image` (o `<img>`) con opacity cross-fade manejada por scroll (puede usar el mismo ScrollTrigger con scrub para setear opacidades) o, si `prefersReducedMotion`, simplemente una grilla/stack estático legible de las imágenes con sus labels (sin pin, sin scrub). Debe verse bien y ser accesible.

- [ ] **Step 1..N** — implementar `showcase.module.css`, `ShowcaseFallback.tsx`, `Showcase.tsx` según los requisitos. Usar `react-hooks/set-state-in-effect` disable justificado si hace falta para el `setActive` inicial. Verificar `npm run typecheck && npm run build && npm run lint`.

- [ ] **Step final: Commit**

```bash
git add -A
git commit -m "feat: seccion Showcase WebGL pineada + fallback CSS con capability gating"
```

---

### Task 4: Integrar Showcase en el home + verificación

**Files:**
- Modify: `src/app/page.tsx` (insertar `<Showcase/>` entre `<Blueprint/>` y `<FeaturedWork/>`)

**Interfaces:**
- Consumes: `<Showcase/>` (Task 3).
- Produces: home con la escena WebGL integrada en el flujo narrativo (Arrival → Blueprint → **Showcase (morph de escenas)** → Living Spaces → Sanctum → Welcome).

- [ ] **Step 1: Insertar en `page.tsx`**

```tsx
import { Hero } from "@/components/sections/Hero";
import { Blueprint } from "@/components/sections/Blueprint";
import { Showcase } from "@/components/sections/Showcase";
import { FeaturedWork } from "@/components/sections/FeaturedWork";
import { Statement } from "@/components/sections/Statement";
import { Welcome } from "@/components/sections/Welcome";
import { Footer } from "@/components/sections/Footer";

export default function HomePage() {
  return (
    <>
      <Hero />
      <Blueprint />
      <Showcase />
      <FeaturedWork />
      <Statement />
      <Welcome />
      <Footer />
    </>
  );
}
```

- [ ] **Step 2: Verificar build + suite + lint + smoke**

Run:
```bash
npm run build && npm run typecheck && npm test && npm run lint
```
Expected: todo verde. Luego smoke: `npm run start` en background, `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000` → 200, y frenar el server.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "feat: integrar Showcase WebGL en el home"
```

---

## Self-Review (writing-plans)

- **Cobertura del spec §5/§6/§7/§20 (efecto de transformación, WebGL/shaders, degradación):** Task 1 (shaders + progreso + deps), Task 2 (SceneTransition/SceneCanvas reutilizables, un solo canvas, lazy), Task 3 (Showcase pineada scroll-driven + fallback CSS + capability gating por touch/low-end/reduced-motion), Task 4 (integración narrativa). 3D pesado / postprocessing quedan fuera (YAGNI; solo el morph que aporta).
- **Placeholders:** Tasks 1 y 2 traen el código completo (shaders + componentes). Task 3 va por requisitos (sección con pin/scrub + fallback; depende de medidas de layout que se validan en browser). Task 1 trae el único test de lógica pura (`mapProgress`).
- **Consistencia de tipos/nombres:** `mapProgress`/`ProgressPair` (Task 1) usados en Task 2; `planeVertexShader`/`displacementFragmentShader` (Task 1) en Task 2; `SceneTransition`/`SceneCanvas` (Task 2) consumidos por `Showcase` (Task 3); `Showcase` (Task 3) montado en Task 4. `progressRef: React.RefObject<number>` consistente entre Tasks 2 y 3.
